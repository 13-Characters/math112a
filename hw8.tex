\documentclass{ben}
\DeclareMathOperator{\erf}{erf}

\title{MATH 112A Homework 8}
\date{November 28, 2025}
\author{Benjamin Tong}
\begin{document}
  \maketitle
  \section{Problem 1}
  Solve the diffusion equation:
  \[
    \begin{cases}
      u_{t} = ku_{xx}, & x > 0, t > 0\\
      u(x, 0) = \sinh x, & x > 0\\
      u(0, t) = 0, & t \ge 0.
    \end{cases}
  \]
  \begin{solution}
    We use the formula that we achieve after doing the odd extension approach, which is
    \[
      u(x, t) = \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty} \left( 
      e^{\frac{-(x - y)^2}{4kt}} - e^{\frac{-(x + y)^2}{4kt}}\right) \sinh y\ dy
    \]
    Now, we note that $\sinh y = \frac{e^y - e^{-y}}{2}$ and we get that
    \begin{align*}
      u(x, t) &= \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty} \left( 
      e^{\frac{-(x - y)^2}{4kt}} - e^{\frac{-(x + y)^2}{4kt}}\right) \sinh y\ dy\\
              &= \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty} \left(
              e^{\frac{-(x - y)^2}{4kt}} - e^{\frac{-(x + y)^2}{4kt}}\right)
              \left( \frac{e^{y}}{2} - \frac{e^{-y}}{2} \right)\ dy\\
              &= \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty} \left( e^{\frac{-(x - y)^2}{4kt}}
              \right) \left( \frac{e^{y}}{2} - \frac{e^{-y}}{2} \right)
              \ dy - \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty} \left( e^{\frac{-(x + y)^2}{4kt}}
              \right) \left( \frac{e^{y}}{2} - \frac{e^{-y}}{2} \right)\ dy\\
              &= \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty}
              \frac{e^{\frac{-(x - y)^2}{4kt} + y}}{2} - \frac{e^{\frac{-(x - y)^2}{4kt} - y}}{2}\ 
              dy - \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty}
              \frac{e^{\frac{-(x + y)^2}{4kt} + y}}{2} - \frac{e^{\frac{-(x + y)^2}{4kt} - y}}{2}\
              dy\\
              &= \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty} \frac{e^{\frac{-(x - y)^2}{4kt} + y}}{2}
              \ dy
              - \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty} \frac{e^{\frac{-(x - y)^2}{4kt} - y}}{2}
              \ dy
              - \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty} \frac{e^{\frac{-(x + y)^2}{4kt} + y}}{2}
              \ dy\\
              &+ \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty} \frac{e^{\frac{-(x + y)^2}{4kt} - y}}{2}
              \ dy
    \end{align*}
    We find that all these integrals are of the general form
    \[
      \frac{1}{\sqrt{4\pi kt}} \int_{0}^{\infty} \frac{e^{\frac{-(y - ax)^2}{4kt} + by}}{2}\ dy
    \]
    We find that we can rewrite the exponent as
    \begin{align*}
      \frac{-(y - ax)^2}{4kt} + by &= \frac{4bkty - y^2 + 2axy - x^2}{4kt}\\
      &= \frac{-y^2 + (2ax + 4bkt)y - x^2 + (ax + 2bkt)^2 - (ax + 2bkt)^2}{4kt}\\
      &= \frac{-(y - ax - 2bkt)^2 + (ax + 2bkt)^2 - x^2}{4kt}\\
      &= \frac{-(y - ax - 2kbt)^2 + (a^2 - 1)x^2 + 4abktx + 4 b^2 k^2 t^2}{4kt}\\
      &= \frac{-(y - ax - 2kbt)^2 + (a^2 - 1)x^2}{4kt} + (abx + bkt)
    \end{align*}
    If $|a| = 1$, which is true of all the integrals, then $a^2 - 1 = 0$,
    and so we can simplify this expression down to
    \[
      \frac{-(y - ax - 2kbt)^2}{4kt} + (abx + bkt)
    \]
    We now have the integral
    \[
      \frac{e^{abx + bkt}}{2\sqrt{4\pi kt}} \int_{0}^{\infty} e^{\frac{-(y - ax - 2bkt)^2}{4kt}}\ dy
    \]
    We apply the change of variables $\alpha = \frac{y - ax - 2bkt}{\sqrt{4kt}}$ and we get that
    \begin{align*}
      \frac{1}{2\sqrt{4\pi kt}} \int_{0}^{\infty} e^{\frac{-(y - ax - 2bkt)^2}{4kt}}\ dy
      &= \frac{e^{(abx + bkt)}}{2\sqrt{\pi}} \int_{\frac{-ax - 2bkt}{\sqrt{4kt}}}^{\infty}
      e^{-\alpha^2}\ d\alpha\\
      &= \frac{e^{(abx + bkt)}}{4} \left(1 - \erf \left( \frac{-ax - 2bkt}{\sqrt{4kt}}
      \right) \right)\\
      &= \frac{e^{(abx + bkt)}}{4} \left(1 + \erf \left( \frac{ax + 2bkt}{\sqrt{4kt}} \right) \right)
    \end{align*}
    Using this generalized formula we get that $u(x, t)$ can be rewritten as
    \begin{align*}
      u(x, t) &= \frac{e^{(x+kt)}}{4} \left(1+\erf \left(\frac{x+2kt}{\sqrt{4kt}}\right)\right)
               - \frac{e^{(-x-kt)}}{4} \left(1+\erf \left(\frac{x-2kt}{\sqrt{4kt}}\right)\right)\\
              &- \frac{e^{(-x+kt)}}{4} \left(1+\erf \left(\frac{-x+2kt}{\sqrt{4kt}}\right)\right)
               + \frac{e^{(x-kt)}}{4} \left(1+\erf \left(\frac{-x-2kt}{\sqrt{4kt}}\right)\right)
    \end{align*}
    We now take advantage of the fact that $\erf$ is odd. We rewrite this as
    \begin{align*}
      u(x, t) &= \frac{e^{(x+kt)}}{4} \left(1+\erf \left(\frac{x+2kt}{\sqrt{4kt}}\right)\right)
               - \frac{e^{-(x+kt)}}{4} \left(1+\erf \left(\frac{x-2kt}{\sqrt{4kt}}\right)\right)\\
              &- \frac{e^{-(x-kt)}}{4} \left(1-\erf \left(\frac{x-2kt}{\sqrt{4kt}}\right)\right)
               + \frac{e^{(x-kt)}}{4} \left(1-\erf \left(\frac{x+2kt}{\sqrt{4kt}}\right)\right)
    \end{align*}
    Now, according to Mathematica, I am wrong, and the solution is actually
    $u(x, t) = e^{kt} \sinh(x)$. We can check that this is the solution by computing
    $u_{xx}$ and $u_t$. We find that
    \begin{align*}
      u &= \sinh(x) \cdot e^{kt}\\
      u_{x} &= \cosh(x) \cdot e^{kt}\\
      u_{xx} &= \sinh(x) \cdot e^{kt}\\
      u_t &= k \cdot \sinh(x) \cdot e^{kt}
    \end{align*}
    This satisfies $u_{xx} = ku_t$, so $u(x, t) = e^x \sinh(x)$ is the correct solution.\\
    Before I end this problem, I would like to give a shoutout to Desmos for
    sanity checking my solution.
  \end{solution}
  \newpage
  \section{Problem 2}
  Solve the diffusion equation:
  \[
    \begin{cases}
      u_{t} = k u_{xx}, & x > 0, t > 0\\
      u(x, 0) = x, & x > 0\\
      u_{x}(0, t) = 0, & t \ge 0.
    \end{cases}
  \]
  First, we use the equation
  \[
  u(x, t) = \frac{1}{\sqrt{4 \pi kt}} \int_{0}^{\infty}
  \left(e^{\frac{-(x - y)^2}{4kt}} + e^{\frac{-(x + y)^2}{4kt}}\right)y \ dy
  \]
  then we split the integral into two parts:
  \[
  u(x, t) = \frac{1}{\sqrt{4 \pi kt}} \left( \int_{0}^{\infty} y e^{\frac{-(x - y)^2}{4kt}}\ dy
  + \int_{0}^{\infty} y e^{\frac{-(x + y)^2}{4kt}}\ dy \right)
  \]
  We now need to solve the integrals $\int_{0}^{\infty} y e^{-\frac{(x - y)^2}{4kt}}\ dy$ and
  $\int_{0}^{\infty} y e^{-\frac{(x + y)^2}{4kt}}\ dy$
  First, we solve the integral $\int_{0}^{\infty} y e^{\frac{-(x - y)^2}{4kt}}\ dy$
  Before we integrate by substitution, we substitute
  $y = \frac{(y - x)}{\sqrt{4kt}} \cdot \sqrt{4kt} + x$., and the exponent is
  $\frac{-(y - x)^2}{4kt}$. Now the integral is
  \[
  \int_{0}^{\infty} \left( \frac{(y - x)}{\sqrt{4kt}} \cdot \sqrt{4kt} + x \right)
  e^{\frac{-(y - x)^2}{4kt}}\ dy
  \]
  Now we make the substitution $\alpha = \frac{(y - x)}{\sqrt{4kt}}$ and we get
  \begin{align*}
    &= \int_{\frac{-x}{\sqrt{4kt}}}^{\infty} \left(\sqrt{4kt} \alpha + x\right) e^{-\alpha^2}\ (\sqrt{4kt} d\alpha)\\
    &= \int_{\frac{-x}{\sqrt{4kt}}}^{\infty} \left(4kt\alpha + \sqrt{4kt}x\right) e^{-\alpha^2}\ d\alpha\\
    &= 4kt \int_{\frac{-x}{\sqrt{4kt}}}^{\infty} \alpha e^{-\alpha^2}\ 
    + \sqrt{4kt} x \int_{\frac{-x}{\sqrt{4kt}}}^{\infty} e^{-\alpha^2}\ d\alpha\\
    &= 4kt \Bigg(-\frac{1}{2}e^{-\alpha^2}\Bigg|_{\frac{-x}{\sqrt{4kt}}}^{\infty} \Bigg)
    + x \sqrt{4kt\pi} \left(\frac{1}{2} + \frac{1}{2} \erf\left(\frac{x}{\sqrt{4kt}}\right)\right)\\
    &= 2kt\left(e^{\frac{-x^2}{4kt}}\right) + x\sqrt{kt\pi} \left(1 + \erf \left(\frac{x}{\sqrt{4kt}}\right)\right)
  \end{align*}
  Now, we find the integral $\int_{0}^{\infty} y e^{-\frac{(x + y)^2}{4kt}}$. Like last time, we
  make the substitution $y = \frac{y + x}{\sqrt{4kt}} \cdot \sqrt{4kt} - x$. We get
  \[
    \int_{0}^{\infty} \left(\frac{y + x}{\sqrt{4kt}} \cdot \sqrt{4kt} - x\right)
    e^{\frac{-(y + x)^2}{4kt}}\ dy
  \]
  Now, let $\beta = \frac{y + x}{\sqrt{4kt}}$ and we get
  \begin{align*}
    &= \int_{\frac{x}{\sqrt{4kt}}}^{\infty}
    \left(\sqrt{4kt} \beta - x\right)e^{-\beta^2}\ (\sqrt{4kt} d\beta)\\
    &= \int_{\frac{x}{\sqrt{4kt}}}^{\infty}
    \left(4kt\beta-\sqrt{4kt}x\right)e^{-\beta^2}\ d\beta\\
    &= 4kt \int_{\frac{x}{\sqrt{4kt}}}^{\infty} \beta e^{-\beta^2}
    - \sqrt{4kt}x \int_{\frac{x}{\sqrt{4kt}}}^{\infty} e^{-\beta^2}\\
    &= 4kt \left(-\frac{1}{2}e^{-\beta^2} \Bigg|_{\frac{x}{\sqrt{4kt}}}^{\infty}\right)
    -\sqrt{4kt}x \left(\frac{1}{2}-\frac{1}{2}\erf \left(\frac{x}{\sqrt{4kt}}\right)\right)\\
    &= 2kt \left(e^{\frac{-x^2}{4kt}}\right)
    + x\sqrt{kt \pi}\left(\erf\left(\frac{x}{4kt}\right) - 1\right)
  \end{align*}
  When we substitute these values into the equation for $u(x, t)$ we get
  \begin{align*}
    u(x, t) &= \frac{1}{\sqrt{4\pi kt}} \left(
      \left(
        2kt \left(e^{-\frac{-x^2}{4kt}}\right)
        + x \sqrt{kt\pi} \left(1 + \erf \left(\frac{x}{4kt}\right)\right)
      \right)
      + \left(
        2kt \left(e^{-\frac{x^2}{4kt}}\right)
        + x \sqrt{kt\pi} \left(\erf \left(\frac{x}{4kt} - 1\right)\right)
      \right)
    \right)\\
    &= \frac{1}{\sqrt{4\pi kt}} \left(
      4kt \left(e^{\frac{-x^2}{4kt}}\right)
      + x \sqrt{kt\pi} \left(
        \left(1 + \erf\left(\frac{x}{4kt}\right)\right)
        + \left(\erf\left(\frac{x}{4kt}\right) - 1\right)
      \right)
    \right)\\
    &= \frac{1}{\sqrt{4\pi kt}} \left(
      4kt \left(e^{\frac{-x^2}{4kt}}\right)
      + 2x \sqrt{kt \pi} \erf \left(\frac{x}{4kt}\right)
    \right)\\
    &= \frac{\sqrt{4kt}}{\sqrt{\pi}} e^{\frac{-x^2}{4kt}} + x \erf \left(\frac{x}{4kt}\right)
  \end{align*}
  \newpage
  \section{Problem 3}
  Consider the following problem with a Robin boundary condition:
  \[
    \begin{cases}
      u_{t} = ku_{xx}, & 0 < x < \infty, t > 0\\
      u(x, 0) = x, & 0 < x < \infty\\
      u_{x}(0, t) - 2u(0, t) = 0, & t \ge 0.
    \end{cases}
  \]
  The purpose of this exercise is to verify the solution formula for this equation. Let $f(x) = x$
  for $x > 0$, let $f(x) = x + 1 - e^{2x}$ and let
  \[
    v(x, t) = \frac{1}{\sqrt{4 \pi kt}} \int_{-\infty}^{\infty} e^{-\frac{(x - y)^{2}}{4kt}}
    f(y)\ dy
  \]
  \begin{enumerate}
    \item What PDE and initial condition does $v(x, t)$ satisfy for $x \in (-\infty, \infty)$?
    \footnote{The homework PDF says $(\infty, \infty)$. I assumed it was a typo and interpreted
    it as $(-\infty, \infty)$}.
    \item Let $w = v_{x} - 2v$. What PDE and initial condition does $w$ satisfy for
    $-\infty < x < \infty$?
    \item Show that $f'(x) - 2f(x)$ is an odd function (for $x \neq 0$).
    \item Show that $w$ is an odd function of $x$.
    \item Assume uniqueness, deduce that $v(x, t)$ solves the equation for $x > 0$.
  \end{enumerate}
  \begin{solution}
    First, we find that $v(x, t)$ satisfies the initial value problem
    \[
    \begin{cases}
      v_t = kv_{xx}, & -\infty < x < \infty, t > 0\\
      v(x, 0) = x + 1 - e^{2x}, & 0 < x < \infty
    \end{cases}
    \]
    now let $w = v_x - 2v$. $w(x, t)$ satisfies the initial value problem
    \[
    \begin{cases}
    \end{cases}
    \]
  \end{solution}
  \section{Problem 4}
  Solve
  \[
    \begin{cases}
      u_{tt} = 4u_{xx}, & 0 < x < \infty,\\
      u(x, 0) = 1, u_{t}(x, 0) = 0, x > 0,\\
      u(0, t) = 0, t \ge 0.
    \end{cases}
  \]
  Using the reflection method. This solution has a singularity; find its location.
  \section{Problem 5}
  Consider the wave equation
  \[
    \begin{cases}
      u_{tt} = u_{xx}, & 0 < x < 1, t > 0.\\
      u(x, 0) = x^{2}(1 - x), u_{t}(x, 0) = (1 - x)^{2}, & 0 < x < 1.\\
      u(0, t) = u(1, t) = 0, & t \ge 0.
    \end{cases}
  \]
  Find $u(\frac{2}{3}, 2)$ and $u(\frac{1}{4}, \frac{7}{2})$.
\end{document}

